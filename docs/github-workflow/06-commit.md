# コミットする

> [← 目次に戻る](README.md) | [前: 05 作業ブランチを作る](05-create-branch.md) | [次: 07 pushする](07-push.md)

## このページで扱うこと

**コミット** は、変更を1つのまとまりとして記録する操作です。
このページでは、変更をコミットする手順を説明します。
コミットした変更をリモートへ送る操作（push）は、次の [07 pushする](07-push.md) で扱います。

## なぜコミットをするのか

コミットは、変更を単位ごとに記録し、あとで見直したり元に戻したりできるようにするためのものです。
ファイルを保存しただけでは履歴が残らないため、記録にはコミットを使います。

## 変更する

ブランチ上でファイルを編集します。
編集する内容はIssueの「やること」に沿います。

## コミットする

変更を **ステージ**（コミット対象として選んだ変更を置く場所）に追加し、コミットします。

```bash
git add <ファイル名>
git commit
```

`git commit` を実行すると、コミットメッセージを書くためのエディタが開きます。
メッセージを書いて保存・終了すると、コミットされます。
エディタを使わずに済ませるなら、`git commit -m "メッセージ"` と直接指定できます。

`git add` には、変更したファイルを指定します。
まとめて追加するときは `git add .` も使えますが、意図しないファイルを混入しやすいため、ファイルを指定する方が安全です。

コミットメッセージは、変更内容が分かる短い文を日本語で書きます。
例をいくつか示します。

```text
Issueテンプレートを追加
コンテスト一覧取得のAPIの実装
MVPのドキュメントを更新
```

メッセージの書き方は [CONTRIBUTING.md の「コミットメッセージ」](../CONTRIBUTING.md#コミットメッセージ) を参照してください。

コミットの前に、変更を小さく保つことを意識します。
1つのコミットに詰め込みすぎると、あとのレビューが難しくなります。

## 次のステップ

コミットした変更を、リモート（GitHub）へ送ります。
[07 pushする](07-push.md) へ進んでください。

## 関連

- [CONTRIBUTING.md - コミットメッセージ](../CONTRIBUTING.md#コミットメッセージ)
- [07 pushする](07-push.md)：コミットした変更をリモートへ送る
