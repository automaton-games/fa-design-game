## Agent skills

### Issue tracker

IssueはGitHub Issuesで管理し、外部Pull Requestはトリアージ対象にしません。詳細は `docs/agents/issue-tracker.md` を参照してください。

### Triage labels

5つの標準トリアージラベル（`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`）を使用します。詳細は `docs/agents/triage-labels.md` を参照してください。

### Domain docs

このリポジトリではsingle-context構成を使用します。詳細は `docs/agents/domain.md` を参照してください。
